# vim: ft=yaml
formatter:
    max_line_length: 0
    retain_line_breaks: false
    retain_line_breaks_single: true
    indentless_arrays: false
    # Don't transform string literals into single line (blocks that start with `>`)
    scan_folded_as_literal: true
